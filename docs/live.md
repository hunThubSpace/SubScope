# Live_urls Management

```bash
➜ ./subscope.py live -h
usage: subscope.py live [-h] {add,list,delete} ...

positional arguments:
  {add,list,delete}
    add              Add a live subdomain
    list             List live subdomains
    delete           Delete live subdomains

options:
  -h, --help         show this help message and exit
```
